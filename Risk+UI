export function computeRisk(instability, lipMismatch) {
  const score =
    0.6 * instability +
    0.4 * lipMismatch;

  return Math.min(score, 1);
}

export function computeRisk(instability, lipMismatch) {
  const score =
    0.6 * instability +
    0.4 * lipMismatch;

  return Math.min(score, 1);
}
